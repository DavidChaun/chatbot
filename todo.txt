1.接收图片byte，保存到s3。
    -- ffmpeg 压缩图片，然后请求llm。
2.音频byte，保存s3。
    -- 使用vtt模型，把音频转为文本。


首要目标：
1.支持接收图片，然后请求gpt4o；
2.将聊天记录保存到sqlite；
3.图片先暂时保存在本地


消息处理：
只取用户为AI的且未读的消息作为query，多条未读的话合为一条。
消息上文，取一个小时前的已读消息，过期不候。


-- 接入search api
